import { EstadoStockPipe } from './estado-stock-pipe';

describe('EstadoStockPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoStockPipe();
    expect(pipe).toBeTruthy();
  });
});
