import { ResaltarBajoStock } from './resaltar-bajo-stock';

describe('ResaltarBajoStock', () => {
  it('should create an instance', () => {
    const directive = new ResaltarBajoStock();
    expect(directive).toBeTruthy();
  });
});
